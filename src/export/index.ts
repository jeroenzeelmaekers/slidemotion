// ---------------------------------------------------------------------------
// slidemotion/export — PDF export (Node-only, not for browser)
// ---------------------------------------------------------------------------

export { exportToPDF } from "./pdf.js";
export type { ExportOptions } from "./types.js";
export { SELECTORS } from "./types.js";
